# aidle-skills 用户级配置文件模板
# ─────────────────────────────────────────────────────────────────────
# ⚠️  这只是模板。请勿把真实 key 写在仓库里！
#
# 用法：
#   mkdir -p ~/.aidle
#   cp examples/aidle-config.env.example ~/.aidle/config.env
#   chmod 600 ~/.aidle/config.env
#   # 然后编辑 ~/.aidle/config.env 填入真实值
#
# 优先级（从高到低）：
#   1. 当前进程的环境变量
#   2. ~/.aidle/config.env (本文件)
#   3. ~/.codex/auth.json (Codex CLI 兼容)
#
# 格式：KEY=VALUE 每行一项，# 开头为注释。值可以加 / 不加引号。
# ─────────────────────────────────────────────────────────────────────

# ─── image-gen ───
# AiPai relay API key
AIPAI_API_KEY=

# AiPai relay base URL（默认 https://aipai.vip/v1）
# AIPAI_BASE_URL=https://aipai.vip/v1

# image-gen 默认生成参数（可选）
# IMG_MODEL=gpt-image-2
# IMG_SIZE=1k
# IMG_QUALITY=high
# IMG_N=1
